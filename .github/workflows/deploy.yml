name: 🚀 Deploy to cPanel
on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Sync files via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.acpedia.id
        username: cegeb@rev.acpedia.id
        password: sellyapp69
        server-dir: ./rev.acpedia.id/
        protocol: ftp