name: 🚀 Trigger cPanel Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Update and Deploy
        run: |
          echo "📥 PULLING CODE..."
          curl -u acpp2118:${{ secrets.CPANEL_PASSWORD }} -v \
          "https://dondang.iixcp.rumahweb.net:2083/execute/VersionControl/update?name=acpedia" > pull_log.json
          cat pull_log.json
          
          echo "🚀 DEPLOYING..."
          curl -u acpp2118:${{ secrets.CPANEL_PASSWORD }} -v \
          "https://dondang.iixcp.rumahweb.net:2083/execute/VersionControl/deploy?name=acpedia" > deploy_log.json
          cat deploy_log.json