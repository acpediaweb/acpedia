name: 🚀 Trigger cPanel Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Update and Sync
        run: |
          # We only run the Update command because your server doesn't support 'deploy'
          echo "📥 PULLING CODE & TRIGGERING SYNC..."
          curl -L -u "acpp2118:${{ secrets.CPANEL_PASSWORD }}" \
          "https://dondang.iixcp.rumahweb.net:2083/execute/VersionControl/update?repository_root=%2fhome%2facpp2118%2f%2fpublic_html%2frev.acpedia.id"